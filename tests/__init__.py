"""
tests/ — Dependency-free checks, run with plain CPython.

There is no pytest in this project's environment and no build step, so each
module exposes `main()` and asserts its way through.  Run them all with:

    python3 -m tests.run
"""
